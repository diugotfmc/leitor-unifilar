# App Streamlit – Leitor de Unifilar (DW)
streamlit>=1.32,<2

# PDF parsing por coordenadas (usa pdfminer.six por baixo)
pdfplumber>=0.11.0,<0.12
pdfminer.six>=20221105

# DataFrames e exportação Excel
pandas>=2.1,<3
numpy>=1.26,<3
openpyxl>=3.1,<4

# Imagens/fontes (dependência comum de PDF/Streamlit)
Pillow>=10.0,<11
